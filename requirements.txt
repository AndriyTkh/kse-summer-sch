# Phase 1 MVP — CPU only
pandas
numpy
lightgbm
prophet
scikit-learn
matplotlib

# Phase 3 (quantile intervals Bq + drift/auto-retrain) — NO new deps:
#   quantile regression = lightgbm objective="quantile"; PSI = numpy; metrics = sklearn/numpy.

# Phase 2 (duration / survival)
# lifelines

# Stretch (C/GRU comparison) — RTX 4060, not MVP
# torch
